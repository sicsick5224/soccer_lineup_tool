# Codex 최적화 구현 프롬프트 v2 — 조기축구 포지션 배치 / 포메이션 / 출전 시간 분배 개인용 모바일 웹 툴

아래 요구사항을 기준으로 **개인 사용용 모바일 우선 웹 앱 MVP**를 구현해줘.  
이 문서는 **Codex가 바로 구현 작업에 들어갈 수 있도록 범위, 규칙, 산출물 형식까지 명확히 제한한 실행용 프롬프트**다.

---

## 0. 최우선 지시

- **MVP에 집중**
- **과설계 금지**
- **백엔드/인증/서버/DB 연동 금지**
- **브라우저 로컬 저장 기반으로만 구현**
- **모바일에서 실제 사용하기 쉬운 UI 우선**
- **핵심은 UI보다 편성 로직의 일관성**
- **코드는 바로 실행 가능한 수준으로 작성**
- **중요 규칙은 코드 주석으로 남길 것**

이 프로젝트는 “예쁘게 보이는 데모”가 아니라,  
**실제로 개인이 조기축구 운영에 사용할 수 있는 편성 툴 MVP**를 만드는 작업이다.

---

## 1. 제품 한 줄 정의

조기축구 운영자가 선수 명단과 포메이션을 입력하면,  
4쿼터 기준 기본 편성안을 자동 생성하고,  
전체 선수의 **필드 플레이 출전 시간 편차를 최대한 줄이며**,  
필요 시 운영자가 수동 수정할 수 있는 **모바일 우선 개인용 웹 앱**을 만든다.

---

## 2. 기술 스택 및 구현 기준

다음 기준으로 구현해줘.

### 필수
- React
- TypeScript
- 브라우저 로컬 저장(localStorage 우선, 필요 시 IndexedDB 가능)
- 모바일 우선 반응형 UI

### 구현 원칙
- 복잡한 상태관리 라이브러리 없이도 충분하면 단순 구조 사용
- 핵심 로직은 UI와 분리
- 순수 함수 중심으로 작성
- 타입 정의 명확히 작성
- 폴더 구조는 실용적으로 유지
- MVP 수준에서 유지보수 가능한 코드로 작성

### 권장
- Vite + React + TypeScript
- 컴포넌트/유틸/타입 분리
- CSS는 단순하고 일관되게
- 외부 라이브러리 의존성은 최소화

---

## 3. 절대 바꾸지 말아야 할 제품 규칙

### 경기 규칙
- 11인제만 지원
- 경기 = 4쿼터
- 1쿼터 = 25분
- 총 경기 시간 = 100분
- 경기 전체에서 포메이션은 1개만 사용
- 자동 편성은 **쿼터 단위 기본안만 생성**
- 경기 중간 교체는 **자동 최적화하지 않음**
- 경기 중간 교체는 **사용자 수동 편집 대상**

### 지원 포지션
- GK
- CB
- LB
- RB
- DM
- CM
- AM
- LW
- RW
- ST

### 포지션 규칙
- 주 포지션은 1개만
- 서브 포지션은 0개 이상
- 서브 포지션에 **GK는 없음**
- 포지션 입력은 자유 입력 금지
- 반드시 선택형 입력

### 지원 포메이션
아래 5개만 지원:

1. 4-2-3-1  
   `GK - LB - CB - CB - RB - DM - DM - LW - AM - RW - ST`

2. 4-4-2  
   `GK - LB - CB - CB - RB - LW - CM - CM - RW - ST - ST`

3. 4-3-3  
   `GK - LB - CB - CB - RB - CM - CM - CM - LW - ST - RW`

4. 3-5-2  
   `GK - CB - CB - CB - DM - DM - LW - CM - RW - ST - ST`

5. 3-4-3  
   `GK - CB - CB - CB - LW - CM - CM - RW - ST - ST - ST`

---

## 4. GK 처리 규칙 — 매우 중요

이 규칙은 반드시 정확히 구현해야 한다.

### GK 배치 규칙
- 주 포지션 GK가 1명 이상 있으면 해당 선수들 내에서 우선 배치
- 주 포지션 GK가 없으면 사용자가 지정한 후보군을 기준으로 순환
- 서브 포지션 GK는 존재하지 않음

### 출전 시간 계산 규칙
- 전체 시간 균등화 기준은 **필드 플레이 출전 시간**
- 주 포지션이 GK가 아닌 선수가 임시로 GK를 맡은 시간은 **필드 출전 시간 계산에서 제외**
- 주 포지션 GK 선수의 GK 출전 시간은 정상 출전으로 계산

### 예시
- 홍길동(ST)
- 1Q~3Q: ST 출전
- 4Q: GK 출전

이 경우 홍길동의 **필드 출전 쿼터 수는 3쿼터**로 계산해야 한다.

이 규칙이 깨지면 제품 핵심 요구사항을 만족하지 못한다.

---

## 5. 자동 편성 목표

시스템은 포메이션 슬롯을 기준으로 4쿼터 기본 라인업을 생성해야 한다.  
자동 편성의 최우선 목표는 아래와 같다.

### 자동 편성 우선순위
1. 전체 선수의 **필드 출전 시간 편차 최소화**
2. 주 포지션 일치
3. 서브 포지션 일치
4. 불가피할 경우 임시 배치 허용

### 해석 기준
- 완전한 수학 최적해보다 **이해 가능한 규칙 기반 결과**를 우선
- 결과는 사용자가 보고 수정하기 쉬워야 함
- 결과는 재현 가능해야 함
- 모바일 환경에서 빠르게 계산되어야 함

---

## 6. 구현 범위

## 6.1 팀 명단 관리
반드시 구현:
- 팀 명단 생성
- 팀 명단 수정
- 팀 명단 삭제
- 선수 추가
- 선수 수정
- 선수 삭제
- 팀 명단 저장
- 팀 명단 불러오기
- 저장된 팀 명단 목록 보기

### 선수 입력 필드
- 이름
- 주 포지션 1개
- 서브 포지션 여러 개 (GK 제외)

### 검증
- 이름 필수
- 주 포지션 필수
- 서브 포지션은 선택
- 주 포지션과 서브 포지션 중복 방지
- 동일 이름 중복은 초기 버전에서 비허용 처리 권장

---

## 6.2 경기 계획 생성
반드시 구현:
- 팀 명단 선택
- 포메이션 선택
- 주 포지션 GK 존재 여부 판단
- 필요 시 GK 후보 지정
- 자동 편성 실행
- 결과 저장
- 저장된 경기 계획 불러오기/삭제

### 자동 편성 전 검증
- 선수 수 11명 미만이면 실행 금지
- 주 포지션 GK가 없는데 GK 후보가 비어 있으면 실행 금지
- 저장 제목이 비어 있으면 저장 금지

---

## 6.3 자동 편성
반드시 구현:
- 4쿼터 기본 편성 생성
- 쿼터별 라인업 생성
- 벤치 인원 계산
- 선수별 필드 출전 쿼터 수 계산
- 임시 GK 배정 여부 계산

### 자동 편성 로직 권장 흐름
1. 입력 데이터 검증
2. 포메이션 슬롯 생성
3. GK 슬롯 우선 배치
4. 각 선수 누적 필드 출전량 계산
5. 출전량이 낮은 선수 우선 후보화
6. 동률이면
   - 주 포지션 일치 우선
   - 서브 포지션 일치 우선
   - 임시 배치 허용
7. 쿼터별 반복
8. 최종 결과 생성

### 주의
- 경기 중간 교체는 자동으로 최적화하지 말 것
- 쿼터 단위 기본안만 자동 생성할 것

---

## 6.4 수동 수정
반드시 구현:
- 쿼터별 슬롯 클릭 후 선수 변경
- 특정 쿼터의 특정 포지션 변경
- 특정 슬롯 고정
- 고정 조건 유지한 채 재계산
- 중간 교체 이벤트 수동 추가
- 중간 교체 이벤트 수정/삭제

### 중간 교체는 이렇게 다룰 것
- 편성 계산의 기본 단위는 쿼터
- 중간 교체는 보조 정보/수동 운영 정보
- MVP에서는 중간 교체가 전체 자동 편성 로직을 복잡하게 깨지 않도록 설계
- 즉, 기본 편성은 유지하고, 사용자가 운영 메모처럼 추가할 수 있도록 구현해도 됨

---

## 6.5 저장
반드시 구현:
- 팀 명단 저장
- 경기 계획 저장
- 목록 조회
- 불러오기
- 삭제

### 저장 방식
- localStorage 우선
- 저장 키 이름은 일관되게 관리
- 저장/불러오기 유틸 함수 분리

---

## 7. 화면 구조 요구사항

모바일 기준으로 다음 4개 흐름이 명확해야 한다.

### 화면 1: 팀 명단 관리
필수 요소:
- 팀 명단 목록
- 새 명단 생성
- 선수 리스트
- 선수 추가 버튼
- 선수 수정/삭제
- 저장 버튼

### 화면 2: 경기 계획 생성
필수 요소:
- 팀 명단 선택
- 포메이션 선택
- GK 자동 감지 결과
- 필요 시 GK 후보 선택 UI
- 자동 편성 버튼

### 화면 3: 편성 결과
필수 요소:
- 1Q / 2Q / 3Q / 4Q 탭
- 현재 쿼터 라인업
- 포메이션 보드 또는 슬롯형 배치 UI
- 벤치 선수 목록
- 선수별 필드 출전 쿼터 수
- 임시 GK 담당 여부
- 저장 버튼

### 화면 4: 수동 수정
필수 요소:
- 슬롯 선택 후 선수 변경
- 슬롯 고정 토글
- 재계산 버튼
- 중간 교체 추가/수정/삭제 UI

---

## 8. 사용자에게 보여줘야 할 결과 정보

반드시 보여줄 것:
- 쿼터별 라인업
- 각 포지션에 누가 배치됐는지
- 벤치 선수 목록
- 선수별 **필드 출전 쿼터 수**
- 임시 GK 담당 여부
- 적합 포지션 부족 시 경고

### 표시 원칙
- “필드 출전”과 “임시 GK”를 혼동하지 않게 표시
- 모바일에서 한눈에 볼 수 있게 단순하게 표시
- 현재 쿼터 중심으로 보되 전체 비교도 가능해야 함

---

## 9. 예외 처리

반드시 구현:
- 선수 수 < 11 → 자동 편성 불가
- 주 포지션 GK 없음 + GK 후보 미지정 → 자동 편성 불가
- 적합 포지션 부족 → 임시 배치 + 경고 표시
- 저장 이름 없음 → 저장 불가
- 잘못된 중간 교체 입력 방지
  - 같은 선수 in/out 금지
  - 존재하지 않는 선수 참조 금지
  - 잘못된 minuteOffset 방지

### 경고 메시지 예시
- “11명 미만이므로 자동 편성을 할 수 없습니다.”
- “주 포지션 GK가 없으므로 GK 후보를 지정해주세요.”
- “일부 포지션은 적합 선수가 부족하여 임시 배치가 적용되었습니다.”

---

## 10. 데이터 구조

아래 구조를 기본으로 구현해줘.  
필요 시 세부 필드는 조금 조정 가능하지만, 의미는 유지해야 한다.

```ts
type Position = 'GK' | 'CB' | 'LB' | 'RB' | 'DM' | 'CM' | 'AM' | 'LW' | 'RW' | 'ST';

interface Player {
  id: string;
  name: string;
  primaryPosition: Position;
  secondaryPositions: Array<'CB' | 'LB' | 'RB' | 'DM' | 'CM' | 'AM' | 'LW' | 'RW' | 'ST'>;
}

interface TeamRoster {
  id: string;
  name: string;
  players: Player[];
  createdAt: string;
  updatedAt: string;
}

type FormationType = '4-2-3-1' | '4-4-2' | '4-3-3' | '3-5-2' | '3-4-3';

interface FormationSlot {
  slotId: string;
  position: Position;
  label: string;
}

interface Assignment {
  slotId: string;
  playerId: string;
}

interface QuarterPlan {
  quarterIndex: 1 | 2 | 3 | 4;
  assignments: Assignment[];
  lockedSlotIds: string[];
}

interface MidGameSubstitution {
  id: string;
  quarterIndex: 1 | 2 | 3 | 4;
  minuteOffset: number;
  outPlayerId: string;
  inPlayerId: string;
  slotId?: string;
  note?: string;
}

interface MatchPlan {
  id: string;
  title: string;
  rosterId: string;
  formation: FormationType;
  quarterDurationMinutes: number;
  quarterCount: number;
  gkCandidates: string[];
  quarterPlans: QuarterPlan[];
  midGameSubstitutions: MidGameSubstitution[];
  createdAt: string;
  updatedAt: string;
}
```

---

## 11. 구현 방식에 대한 구체 지시

### 코드 구조
다음처럼 명확히 나눠줘.
- 타입 정의
- 포메이션 정의 상수
- 자동 편성 로직
- 출전 시간 계산 로직
- 저장 유틸
- UI 컴포넌트
- 메인 앱 조립

### 로직 함수 예시
가능하면 아래 성격의 함수로 나눠줘.
- `getFormationSlots`
- `generateMatchPlan`
- `recalculateMatchPlan`
- `calculateFieldPlayCounts`
- `getBenchPlayers`
- `saveRoster`
- `loadRosters`
- `saveMatchPlan`
- `loadMatchPlans`

### UI 구현 기준
- 탭 UI
- 카드 UI
- 작은 화면에서도 눌리기 쉬운 버튼
- 선택/수정 흐름이 직관적일 것
- 너무 화려할 필요 없음
- 실사용 우선

---

## 12. 산출물 형식 — 꼭 이 순서로 출력

최종 답변은 아래 순서로 구성해줘.

1. 프로젝트 구조
2. 전체 코드
3. 핵심 로직 설명
4. 실행 방법
5. 향후 개선 아이디어

### 추가 요구
- 코드 누락 없이 제공
- 바로 복붙 가능한 수준
- 여러 파일이면 파일별로 구분해서 제공
- 중요한 파일부터 순서 있게 제시
- 실행 명령어 포함

---

## 13. 품질 체크리스트

최종 출력 전에 스스로 아래를 확인하고 반영해줘.

- [ ] 11명 미만 자동 편성 차단이 구현되었는가
- [ ] GK 후보 미지정 예외가 구현되었는가
- [ ] 비GK 선수의 임시 GK 시간이 필드 출전 계산에서 제외되는가
- [ ] 쿼터별 라인업이 생성되는가
- [ ] 수동 수정 후 재계산이 가능한가
- [ ] 팀 명단과 경기 계획을 저장/불러오기 할 수 있는가
- [ ] 모바일에서 보기 쉬운 구조인가
- [ ] 과도한 복잡성 없이 MVP 범위를 지켰는가

---

## 14. 최종 요청

위 조건을 만족하는 **모바일 우선 React + TypeScript 웹 앱 MVP**를 구현해줘.  
설명보다 **작동하는 코드 완성도**를 우선해줘.  
애매한 부분은 MVP 원칙에 맞게 단순하고 일관되게 결정해서 구현해줘.
