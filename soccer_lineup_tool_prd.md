# PRD 초안 — 조기축구 포지션 배치 / 포메이션 / 출전 시간 분배 개인용 모바일 웹 툴

## 1. 문서 개요

- **문서명**: 조기축구 포지션 배치 / 포메이션 / 출전 시간 분배 개인용 모바일 웹 툴 PRD
- **문서 버전**: v0.1 Draft
- **문서 목적**: Codex가 바로 구현에 착수할 수 있도록 제품 목적, 범위, 기능, 데이터 구조, 핵심 로직, 예외 처리, 화면 요구사항을 명확히 정의한다.
- **대상 사용자**: 조기축구 팀 운영자 1인
- **배포 범위**: 개인 사용용 웹 앱
- **우선 디바이스**: 모바일(스마트폰) 우선, 데스크톱에서도 사용 가능

---

## 2. 제품 목적

조기축구 운영자가 선수 명단과 포메이션을 입력하면, 각 쿼터별 포지션 배치와 교체 계획을 자동 생성하고, 전체 선수의 출전 시간을 최대한 공평하게 배분할 수 있도록 돕는 모바일 친화적 개인용 웹 도구를 만든다.

이 제품의 핵심은 단순 명단 관리가 아니라, **선수 명단 + 포메이션 + 경기 시간 조건을 바탕으로 기본 편성안을 자동 생성하고, 운영자가 필요 시 수동 수정할 수 있는 편성 도구**를 만드는 것이다.

---

## 3. 제품 목표

### 3.1 핵심 목표
1. 선수 명단을 빠르게 입력/저장할 수 있어야 한다.
2. 선택한 포메이션에 맞춰 기본 라인업을 자동 생성해야 한다.
3. 전체 선수의 **필드 플레이 출전 시간**이 최대한 공평하도록 배치해야 한다.
4. 자동 생성 결과를 운영자가 직접 수정할 수 있어야 한다.
5. 팀 명단과 경기 계획을 브라우저 로컬 저장 방식으로 저장/불러오기 할 수 있어야 한다.

### 3.2 비목표(초기 버전에서 하지 않는 것)
1. 외부 사용자와 공유하는 협업 기능
2. 로그인/회원가입 기능
3. 서버 DB 연동
4. 경기 전체 전술 분석 기능
5. 실시간 경기 기록 기능
6. 경기 전체에서 여러 포메이션을 혼용하는 기능
7. 자동 중간 교체 최적화 기능
8. 서브 포지션으로 GK를 지정하는 기능

---

## 4. 지원 범위

### 4.1 경기 형식
- **11인제만 지원**
- 기본 경기 구조는 **4쿼터**
- **1쿼터 = 25분**
- 총 경기 시간 = **100분**

### 4.2 포메이션 범위
초기 버전에서 아래 5개 포메이션만 지원한다.

1. **4-2-3-1**  
   `GK - LB - CB - CB - RB - DM - DM - LW - AM - RW - ST`

2. **4-4-2**  
   `GK - LB - CB - CB - RB - LW - CM - CM - RW - ST - ST`

3. **4-3-3**  
   `GK - LB - CB - CB - RB - CM - CM - CM - LW - ST - RW`

4. **3-5-2**  
   `GK - CB - CB - CB - DM - DM - LW - CM - RW - ST - ST`

5. **3-4-3**  
   `GK - CB - CB - CB - LW - CM - CM - RW - ST - ST - ST`

### 4.3 포지션 범위
지원 포지션은 아래 10개로 고정한다.

- GK
- CB
- LB
- RB
- DM
- CM
- AM
- LW
- RW
- ST

**중요 규칙:**
- 주 포지션은 1개만 가진다.
- 서브 포지션은 0개 이상 가질 수 있다.
- **서브 포지션에 GK는 존재하지 않는다.**
- 포지션 입력은 자유 텍스트가 아니라, 사전에 정의된 목록 선택 방식으로 처리한다.

---

## 5. 핵심 사용자 시나리오

### 시나리오 1: 팀 명단 입력
1. 운영자가 팀 선수 명단을 입력한다.
2. 각 선수에 대해 이름, 주 포지션, 서브 포지션을 입력한다.
3. 명단을 저장한다.

### 시나리오 2: 경기 계획 생성
1. 운영자가 저장된 팀 명단을 불러온다.
2. 경기 포메이션을 선택한다.
3. 필요 시 GK 후보를 지정한다.
4. 자동 편성 버튼을 누른다.
5. 시스템이 4쿼터 기준 기본 편성안을 생성한다.
6. 운영자가 결과를 검토한다.

### 시나리오 3: 수동 수정
1. 운영자가 특정 쿼터의 특정 포지션 배치를 수정한다.
2. 필요한 경우 특정 선수를 고정한다.
3. 수정된 조건을 유지한 채 남은 편성안을 다시 계산하거나 그대로 저장한다.

### 시나리오 4: 저장 및 재사용
1. 운영자가 팀 명단을 저장한다.
2. 운영자가 경기 계획을 저장한다.
3. 다음 경기에서 저장된 데이터를 다시 불러온다.

---

## 6. 기능 요구사항

## 6.1 선수 명단 관리

### 필수 기능
- 선수 추가
- 선수 수정
- 선수 삭제
- 팀 명단 저장
- 저장된 팀 명단 불러오기

### 선수 입력 항목
- 이름
- 주 포지션(1개)
- 서브 포지션(복수 선택 가능, 단 GK 제외)

### 검증 규칙
- 이름은 필수 입력
- 주 포지션은 필수 입력
- 서브 포지션은 선택 입력
- 서브 포지션은 주 포지션과 중복될 수 없도록 처리 권장
- 동일 이름 중복 허용 여부는 초기 버전에서 비허용 권장

---

## 6.2 포메이션 선택

### 필수 기능
- 경기 계획 생성 시 포메이션 1개 선택
- 경기 전체에서 포메이션은 1개만 사용

### 규칙
- 경기 중 포메이션 변경은 지원하지 않는다.
- 포메이션은 내부적으로 실제 슬롯 배열로 관리한다.

---

## 6.3 GK 처리

### GK 배치 규칙
- 주 포지션 GK가 1명 이상 존재할 경우, GK 포지션은 해당 선수들 내에서 우선 배치한다.
- 주 포지션 GK가 없을 경우, GK 포지션은 사용자가 사전에 지정한 후보군을 기준으로 순환 배치한다.
- 본 제품에서는 **서브 포지션 GK를 지원하지 않는다.**

### GK 후보 지정 기능
- 주 포지션 GK가 없는 경우에만 GK 후보 지정 UI를 노출한다.
- 운영자는 1명 이상을 GK 후보로 지정할 수 있어야 한다.
- 후보가 지정되지 않은 상태에서는 자동 편성을 실행할 수 없도록 하거나, 명확한 경고를 표시해야 한다.

### 출전 시간 계산 규칙
- 출전 시간 균등화는 **필드 플레이 기준**으로 계산한다.
- 주 포지션이 GK가 아닌 선수가 임시로 GK를 맡은 시간은 개인 출전 쿼터 및 출전 시간 계산에 포함하지 않는다.
- 주 포지션 GK 선수의 GK 출전 시간은 정상 출전 시간으로 계산한다.

### 예시
- 홍길동(주 포지션 ST)
- 1~3쿼터: ST 출전
- 4쿼터: GK 출전

이 경우 홍길동의 출전 쿼터 수는 **3쿼터**로 계산한다.

---

## 6.4 자동 편성

### 자동 편성의 목적
시스템은 선택된 포메이션 슬롯을 기준으로 4쿼터의 기본 라인업을 생성하되, 전체 선수의 **필드 플레이 출전 시간 편차를 최소화**하는 것을 최우선 목표로 한다.

### 자동 편성 우선순위
1. 전체 선수 출전 시간 편차 최소화
2. 주 포지션 일치
3. 서브 포지션 일치
4. 불가피할 경우 임시 배치 허용

### 자동 편성 규칙
- 11명 미만일 경우 자동 편성 불가
- 11명 이상일 경우에만 자동 편성 가능
- 자동 편성은 기본적으로 **쿼터 단위 기본안**을 생성한다.
- 경기 중간 교체는 자동 계산 대상이 아니라, **수동 편집 대상**으로 취급한다.
- 포지션 적합 선수가 부족할 경우, 우선순위에 따라 대체 배치를 수행한다.
- 특정 슬롯에 적합 선수가 전혀 없을 경우, 임시 배치 또는 경고 표시를 허용한다.

### 11명 미만 예외 처리
- 선수 수가 11명 미만이면 자동 편성을 수행하지 않는다.
- 사용자에게 “11명 미만이므로 자동 편성을 할 수 없습니다.”라는 안내를 표시한다.

---

## 6.5 수동 수정

### 필수 기능
- 특정 쿼터의 포지션 슬롯에 배치된 선수를 변경할 수 있어야 한다.
- 특정 선수를 다른 선수와 교체할 수 있어야 한다.
- 특정 슬롯 또는 특정 선수를 고정할 수 있어야 한다.
- 수정된 상태를 유지한 채 나머지 편성안을 다시 계산할 수 있어야 한다.
- 쿼터 중간 교체 이벤트를 수동으로 추가/수정할 수 있어야 한다.

### 수동 수정 범위
초기 버전에서 아래를 지원한다.
- 포지션 슬롯별 선수 수동 재배치
- 특정 쿼터에만 적용되는 선수 교체
- 사용자가 수정한 슬롯의 고정 처리
- 고정된 조건을 유지한 채 나머지 자동 재생성

### 중간 교체 규칙
- 자동 생성은 쿼터 단위 편성안만 만든다.
- 경기 중간 교체는 운영자가 직접 추가/수정한다.
- 추후 버전에서 자동 중간 교체 추천 기능을 고려할 수 있으나, 초기 버전 범위에는 포함하지 않는다.

---

## 6.6 저장

### 저장 대상
- 팀 명단
- 경기 계획

### 저장 방식
- 브라우저 로컬 저장 방식 사용
- 초기 버전에서는 `localStorage` 또는 `IndexedDB` 기반 구현 권장

### 저장 기능 상세
- 팀 명단 저장
- 저장된 팀 명단 목록 보기
- 팀 명단 불러오기
- 팀 명단 삭제
- 경기 계획 저장
- 저장된 경기 계획 목록 보기
- 경기 계획 불러오기
- 경기 계획 삭제

---

## 7. 데이터 구조 초안

## 7.1 Player

```ts
interface Player {
  id: string;
  name: string;
  primaryPosition: 'GK' | 'CB' | 'LB' | 'RB' | 'DM' | 'CM' | 'AM' | 'LW' | 'RW' | 'ST';
  secondaryPositions: Array<'CB' | 'LB' | 'RB' | 'DM' | 'CM' | 'AM' | 'LW' | 'RW' | 'ST'>;
}
```

## 7.2 TeamRoster

```ts
interface TeamRoster {
  id: string;
  name: string;
  players: Player[];
  createdAt: string;
  updatedAt: string;
}
```

## 7.3 FormationType

```ts
type FormationType = '4-2-3-1' | '4-4-2' | '4-3-3' | '3-5-2' | '3-4-3';
```

## 7.4 FormationSlot

```ts
type Position = 'GK' | 'CB' | 'LB' | 'RB' | 'DM' | 'CM' | 'AM' | 'LW' | 'RW' | 'ST';

interface FormationSlot {
  slotId: string;
  position: Position;
  label: string;
}
```

## 7.5 MatchPlan

```ts
interface MatchPlan {
  id: string;
  title: string;
  rosterId: string;
  formation: FormationType;
  quarterDurationMinutes: number; // 25
  quarterCount: number; // 4
  gkCandidates: string[]; // player ids
  quarterPlans: QuarterPlan[];
  midGameSubstitutions: MidGameSubstitution[];
  createdAt: string;
  updatedAt: string;
}
```

## 7.6 QuarterPlan

```ts
interface QuarterPlan {
  quarterIndex: 1 | 2 | 3 | 4;
  assignments: Assignment[];
  lockedSlotIds: string[];
}

interface Assignment {
  slotId: string;
  playerId: string;
}
```

## 7.7 MidGameSubstitution

```ts
interface MidGameSubstitution {
  id: string;
  quarterIndex: 1 | 2 | 3 | 4;
  minuteOffset: number; // 해당 쿼터 시작 후 분 단위
  outPlayerId: string;
  inPlayerId: string;
  slotId?: string;
  note?: string;
}
```

---

## 8. 자동 편성 로직 가이드

### 8.1 기본 원칙
자동 편성은 완전한 최적해 탐색보다, **실용적인 수준의 합리적인 배치**를 목표로 한다.

### 8.2 권장 계산 흐름
1. 입력 선수 수 검증
2. 포메이션 슬롯 생성
3. GK 배치 우선 처리
4. 각 선수의 현재 누적 필드 출전량 계산
5. 출전량이 적은 선수 우선 배치
6. 같은 출전량일 경우
   - 주 포지션 일치 선수 우선
   - 그다음 서브 포지션 일치 선수 우선
   - 그다음 임시 배치 허용
7. 각 쿼터마다 배치 반복
8. 결과 생성 후 사용자 편집 허용

### 8.3 시간 계산 주의사항
- 주 포지션 GK 선수의 GK 출전은 정상 출전으로 계산
- 주 포지션 GK가 아닌 선수의 GK 출전은 필드 출전 시간 계산에서 제외
- 사용자에게는 필요 시
  - 필드 출전 쿼터 수
  - 임시 GK 담당 쿼터 수
  를 분리 표시할 수 있다.

### 8.4 현실적 구현 원칙
Codex 구현 시 복잡한 수학적 최적화보다 아래 원칙을 우선한다.
- 이해 가능한 규칙 기반 배치
- 재현 가능한 결과
- 사용자가 수정하기 쉬운 결과
- 모바일에서 빠르게 사용할 수 있는 응답 속도

---

## 9. UI/UX 요구사항

## 9.1 공통 원칙
- 모바일 우선 UI
- 한 손 사용 고려
- 입력 흐름 단순화
- 복잡한 설정은 단계별로 노출
- 결과는 표와 포메이션 보드 양쪽에서 이해 가능해야 함

## 9.2 권장 화면 구조

### 화면 1: 팀 명단 관리
- 팀 명단 목록
- 새 팀 명단 생성
- 선수 추가/수정/삭제
- 저장 버튼

### 화면 2: 경기 계획 생성
- 팀 명단 선택
- 포메이션 선택
- 주 포지션 GK 유무 확인
- 필요 시 GK 후보 지정
- 자동 편성 실행 버튼

### 화면 3: 편성 결과
- 쿼터 탭(1Q, 2Q, 3Q, 4Q)
- 포메이션 보드 위 선수 배치
- 벤치 선수 목록
- 선수별 누적 필드 출전 쿼터 수 표시
- 선수별 임시 GK 담당 여부 표시
- 저장 버튼

### 화면 4: 수동 수정
- 슬롯 클릭 후 선수 변경
- 드롭다운 또는 바텀시트로 교체
- 특정 슬롯 고정 토글
- 중간 교체 추가 UI
- 재계산 버튼

---

## 10. 결과 표시 요구사항

### 필수 표시 정보
- 쿼터별 라인업
- 포지션별 배치 결과
- 벤치 선수 목록
- 선수별 필드 출전 쿼터 수
- 주 포지션 GK 여부
- 임시 GK 배정 여부

### 표시 우선순위
1. 현재 쿼터 라인업이 한눈에 보여야 한다.
2. 누가 벤치인지 바로 보여야 한다.
3. 누적 출전 쿼터 수를 쉽게 비교할 수 있어야 한다.
4. 임시 GK 배정은 필드 출전과 혼동되지 않게 표시해야 한다.

---

## 11. 예외 처리 및 경고

### 필수 예외 처리
- 선수 수가 11명 미만인 경우
- 주 포지션 GK가 없는데 GK 후보도 지정되지 않은 경우
- 특정 포메이션 슬롯에 적합 선수가 부족한 경우
- 저장할 팀 명단 이름 또는 경기 계획 이름이 비어 있는 경우
- 잘못된 중간 교체 입력(같은 선수 in/out 등)

### 경고 메시지 예시
- “11명 미만이므로 자동 편성을 할 수 없습니다.”
- “주 포지션 GK가 없으므로 GK 후보를 지정해주세요.”
- “일부 포지션은 적합 선수가 부족하여 임시 배치가 적용되었습니다.”

---

## 12. 기술 구현 가이드

### 권장 기술 방향
- **형태**: 개인용 모바일 웹 앱
- **저장소**: 로컬 저장 기반
- **배포 우선순위**: 로컬 실행 또는 간단한 정적 배포 가능 구조

### Codex 구현 시 권장 사항
- React + TypeScript 기반 권장
- 상태 관리는 초기 버전에서 단순한 구조 우선
- 포메이션 슬롯/편성 로직/시간 계산 로직은 UI와 분리된 순수 함수로 작성
- 저장/불러오기 로직도 별도 유틸로 분리
- 모바일 UI는 단순하고 명확한 카드/탭 구조 사용 권장

---

## 13. MVP 완료 기준

아래 조건을 만족하면 MVP로 본다.

1. 팀 명단을 생성/수정/삭제/저장/불러오기 할 수 있다.
2. 포메이션 5종 중 1개를 선택할 수 있다.
3. 주 포지션 GK 유무에 따라 자동 또는 후보 지정 방식으로 GK를 처리할 수 있다.
4. 11명 이상 선수 기준으로 4쿼터 기본 편성안을 자동 생성할 수 있다.
5. 출전 시간 계산 시 비GK 선수의 임시 GK 출전은 필드 출전 계산에서 제외할 수 있다.
6. 자동 생성 결과를 쿼터별로 확인할 수 있다.
7. 운영자가 수동으로 선수 배치를 수정할 수 있다.
8. 중간 교체를 수동으로 추가/수정할 수 있다.
9. 수정된 결과를 저장하고 다시 불러올 수 있다.

---

## 14. 추후 확장 아이디어

초기 버전 범위에는 포함하지 않지만, 추후 아래 기능을 고려할 수 있다.

- 자동 중간 교체 추천
- 선수별 최소/최대 출전 쿼터 설정
- 특정 선수 고정 출전 옵션
- 특정 포지션 금지 옵션
- 체력/실력 기반 가중치 배치
- 경기별 기록 저장
- 결과 공유/export 기능
- 태블릿 최적화 UI

---

## 15. Codex 전달용 한 줄 요약

모바일 우선 개인용 웹 앱을 만든다. 사용자는 조기축구 팀 선수 명단(이름, 주 포지션 1개, 서브 포지션)을 저장하고, 11인제 기준 포메이션 1개를 선택해 4쿼터(25분 x 4) 기본 편성안을 자동 생성한다. 자동 편성의 최우선 목표는 전체 선수의 **필드 플레이 출전 시간 편차 최소화**이며, 주 포지션 GK가 없을 경우 사용자가 지정한 후보군으로 GK를 순환한다. 단, **주 포지션이 GK가 아닌 선수가 임시 GK를 맡은 시간은 필드 출전 시간 계산에 포함하지 않는다.** 자동 생성 후 사용자는 쿼터별 배치와 중간 교체를 수동 수정하고, 팀 명단과 경기 계획을 로컬 저장할 수 있어야 한다.
